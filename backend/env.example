# Django
SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# GitHub token file path (optional)
# GITHUB_TOKEN_FILE=.github-token

# Supabase PostgreSQL (required) — Database → Connect → Session pooler URI (port 5432)
# DATABASE_URL=postgresql://postgres.PROJECT_REF:your-password@aws-0-eu-central-1.pooler.supabase.com:5432/postgres
