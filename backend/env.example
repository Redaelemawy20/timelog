# Django
SECRET_KEY=change-me
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost

# GitHub token file path (optional)
# GITHUB_TOKEN_FILE=.github-token

# Supabase PostgreSQL (Session pooler or direct connection from Project Settings → Database)
# When set, the API uses Supabase as default and keeps backend/db.sqlite3 as the "sqlite" alias.
# DATABASE_URL=postgresql://postgres.PROJECT_REF:your-password@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
